{
  "name": "watchflix-mobile",
  "version": "1.0.0",
  "description": "Watchflix mobile app (Capacitor)",
  "scripts": {
    "build": "cd ../ui && npm run build && cd ../mobile && npx cap copy",
    "build:android": "npm run build && npx cap open android",
    "build:ios": "npm run build && npx cap open ios",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
